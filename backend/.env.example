# Supabase
SUPABASE_URL=https://zbsbfhmsgrlohxdxihaw.supabase.co
SUPABASE_ANON_KEY=eyJhbGc...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...

# AI Services
ANTHROPIC_API_KEY=sk-ant-...

# OpenRouter Configuration (configurado pelo administrador via painel admin)
# ⚠️ Estas configurações são gerenciadas pelo administrador
# O painel admin ainda não foi desenvolvido
USE_OPENROUTER=false
OPENROUTER_API_KEY=
OPENROUTER_SCRIPT_MODEL=
OPENROUTER_DESCRIPTION_MODEL=
OPENROUTER_ASSISTANT_MODEL=

# Web Search
TAVILY_API_KEY=tvly-...

# Transcription (choose one)
DEEPGRAM_API_KEY=xxx
# WHISPER_MODEL=base  # if using local Whisper

# Encryption (generate with: python -c "import secrets; print(secrets.token_urlsafe(32))")
ENCRYPTION_KEY=your_32_byte_encryption_key_here

# Webhook Secrets (generate with: python -c "import secrets; print(secrets.token_urlsafe(32))")
# ⚠️ Configure este secret no dashboard do HeyGen
# HeyGen: Dashboard > Settings > Webhooks > Secret
HEYGEN_WEBHOOK_SECRET=your_heygen_webhook_secret_here

# Redis (for rate limiting and caching)
# Development: usar Redis local ou Docker
# Production: usar Redis gerenciado (Upstash, Redis Cloud, etc.)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# REDIS_PASSWORD=your_redis_password  # Opcional, apenas se Redis tiver senha

# CORS Configuration
# Lista de origens permitidas separadas por vírgula
# ⚠️ NUNCA use "*" em produção - isso expõe sua API a qualquer origem
# Exemplos:
# - Development: http://localhost:5173,http://localhost:3000
# - Staging: https://staging.renum.app,http://localhost:5173
# - Production: https://renum.app,https://app.renum.com,https://www.renum.com
FRONTEND_URL=https://renum.vercel.app
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,https://renum.vercel.app

# Storage
TEMP_VIDEO_PATH=/tmp/videos

# Server
HOST=0.0.0.0
PORT=8000
ENVIRONMENT=production
DEBUG=false

# Logs
LOG_LEVEL=INFO